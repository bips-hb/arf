# which.max() with random at ties

which.max() with random at ties

## Usage

``` r
which.max.random(x)
```

## Arguments

- x:

  A numeric vector.

## Value

Index of maximum value in x, with random tie-breaking.
