# Rename all problematic columns with col_rename().

Rename all problematic columns with col_rename().

## Usage

``` r
col_rename_all(cn)
```

## Arguments

- cn:

  Old column names.

## Value

New columns names.
